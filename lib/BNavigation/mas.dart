import 'package:flutter/material.dart';

class Mas extends StatelessWidget {
  const Mas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sección para Más"),
    );
  }
}