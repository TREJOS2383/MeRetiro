import 'package:flutter/material.dart';

class Rodar extends StatelessWidget {
  const Rodar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sección MapBox"),
    );
  }
}